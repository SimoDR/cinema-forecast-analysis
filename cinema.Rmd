```{r}
library(XML)
# install.packages('dplyr')
library(dplyr)
library(ggplot2)
library(readr)
```
# Italy

## Italy data

```{r}
it_cinema <- read.csv("italy_data.csv")
it_cinema
```

```{r}
it_cinema$Week <- as.numeric(as.character(it_cinema$Week))
it_cinema$Week <- as.numeric(as.character(it_cinema$Week))
it_cinema$Date <- with(it_cinema, as.Date(paste0(it_cinema$Year, "-01-01"), format="%Y-%m-%d", origin="1970-01-01") +
                                               as.difftime(it_cinema$Week, unit="weeks"))
it_cinema$Overall.Gross <- parse_number(it_cinema$Overall.Gross)
```


```{r,fig.width = 8,dpi=600}
ggplot( data = it_cinema, aes( Date, Overall.Gross )) + geom_line() 
```

# Italian productions in italy

```{r}
ita_prods <- read.csv("italy_prods.csv")
ita_prods
```

```{r}
ita_prods$Date <- with(ita_prods, as.Date(paste0(ita_prods$Year, "-01-01"), format="%Y-%m-%d", origin="1970-01-01") +
                                               as.difftime(ita_prods$Week, unit="weeks"))
```

```{r,fig.width = 8,dpi=600}
ggplot( data = ita_prods, aes( Date, IT_movies )) + geom_line() 
```

# US

## US data
```{r}
us_cinema <- read.csv("us_data.csv")
us_cinema
```

```{r}
us_cinema$Week <- as.numeric(as.character(us_cinema$Week))
us_cinema$Week <- as.numeric(as.character(us_cinema$Week))
us_cinema$Date <- with(us_cinema, as.Date(paste0(us_cinema$Year, "-01-01"), format="%Y-%m-%d", origin="1970-01-01") +
                                               as.difftime(us_cinema$Week, unit="weeks"))
us_cinema$Overall.Gross <- parse_number(us_cinema$Overall.Gross)
```


```{r,fig.width = 8,dpi=600}
ggplot( data = us_cinema, aes( Date, Overall.Gross )) + geom_line() 
```
## Italian productions in US

```{r}
us_prods <- read.csv("italy_prods.csv")
us_prods
```

```{r}
us_prods$Date <- with(us_prods, as.Date(paste0(us_prods$Year, "-01-01"), format="%Y-%m-%d", origin="1970-01-01") +
                                               as.difftime(us_prods$Week, unit="weeks"))
```

```{r,fig.width = 8,dpi=600}
ggplot( data = us_prods, aes( Date, IT_movies )) + geom_line() 
```